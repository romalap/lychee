pipeline {
    parameters {
    choice(name: 'VERSION', choices: ['dev'], description: 'Pick the VERSION')
    }
    agent any 
    stages {
        stage('run_docker') {
            steps {
            sh """
            docker-compose down
            docker-compose up -d
            """
            }
        }
    }
}